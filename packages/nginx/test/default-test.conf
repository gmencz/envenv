upstream test-api-gateway {
  server test-api-gateway:7000;
}

upstream test-auth-service {
  server test-auth-service:5000;
}

upstream test-client {
  server test-client:3000;
}

server {
  listen 80;

  location / {
    proxy_pass http://test-client;
  }

  # websocket config
  location /sockjs-node {
    proxy_pass http://test-client;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "Upgrade";

    # This is so the dev server doesn't disconnect.
    proxy_read_timeout 86400s;
    proxy_send_timeout 86400s;
  }

  location /graphql {
    proxy_pass http://test-api-gateway;
  }

  location /oauth {
    rewrite /oauth/(.*) /$1 break;
    proxy_pass http://test-auth-service;
  }
}